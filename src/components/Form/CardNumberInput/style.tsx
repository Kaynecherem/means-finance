import styled from "styled-components";
import NumberField from "../NumberField";

export const StyledCardNumberInput = styled(NumberField)`

& .ant-input-number-prefix{
        margin-right: 8px;
        & img{
            width: 34px;
        }
    }
`
